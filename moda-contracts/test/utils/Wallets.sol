// SPDX-License-Identifier: MIT
pragma solidity ^0.8.21;

contract Wallets {
    address public alice = address(0x1);
    address public bob = address(0x2);
    address public carl = address(0x3);
    address public dave = address(0x4);
    address public eric = address(0x5);
    address public frank = address(0x6);
    address public gary = address(0x7);
    address public harry = address(0x8);
    address public ian = address(0x9);
    address public karen = address(0xa);
}
